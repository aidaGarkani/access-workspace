import { styled } from "@mui/system";
import Paper from "@mui/material/Paper";

export const Title = styled("div")({
  display: "flex",
  justifyContent: "space-between",
});

export const ACPaper = styled(Paper)({
  padding: 10,
  marginBottom: 5,
});
